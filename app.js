const defaultAbout = {
  title: 'About RR Gold Nath',
  subtitle: 'Nine Generations of Tradition. Six Decades of Trust.',
  history: 'RR Gold Nath carries a jewellery tradition passed down through nine generations. Our family traces its roots to Barmeda, Rajasthan, and belongs to the traditional Sonar community of Rajasthan, with a deep connection to the art and craftsmanship of gold jewellery. The family later settled in Pune, bringing this rich heritage and traditional values with them. Since 1960, we have served customers in Pune with a commitment to quality, craftsmanship, trust, and personal relationships.',
  statement: 'From Barmeda, Rajasthan to Pune — a family legacy in jewellery carried forward across nine generations and serving customers in Pune since 1960.'
};
const values = [['Heritage', 'A jewellery tradition carried forward through nine generations.'], ['Craftsmanship', 'Respect for the skill, detail, and artistry behind every piece.'], ['Trust', 'Building lasting relationships with our customers.'], ['Quality', 'A commitment to quality and attention to detail.'], ['Tradition & Today', 'Preserving our heritage while embracing contemporary tastes.']];
const timeline = ['Barmeda, Rajasthan', 'Sonar Community Heritage', '9 Generations of Craftsmanship', 'Family Settles in Pune', 'Jewellery Business Since 1960', 'RR Gold Nath Today'];
const images = Array.from({ length: 12 }, (_, index) => `Images/${index + 1}.jpeg`);
const products = ['The Barmeda Ring', 'Pune Heritage Nath', 'Dawnlight Pendant', 'Ninefold Bangles', 'Sona Jhumka', 'Rajwadi Necklace', 'Heirloom Kada', 'Mogra Earrings', 'Peshwai Chain', 'Aabha Bracelet', 'Virasat Haar', 'Surya Studs'].map((name, index) => ({ name, image: images[index], category: ['Rings', 'Naths', 'Pendants', 'Bangles'][index % 4] }));
const nathSeed = [
  ['Maharashtrian Nath', ['Marathi Nath', 'Brahmani Nath', 'Peshwai Nath', 'Kolhapuri Nath', 'Pune Nath', 'Traditional Maharashtrian Nath']],
  ['Rajasthani Nath', ['Rajasthani Nath', 'Rajputi Nath', 'Traditional Rajasthani Nath']],
  ['North Indian Nath', ['Punjabi Nath', 'Bengali Nath', 'Bengali Nathni', 'Kashmiri Nath', 'Laung / Laung Nath']],
  ['South Indian Nose Jewellery', ['Mukkuthi / Mookkuthi', 'Traditional South Indian Nose Stud', 'South Indian Nose Pin']],
  ['Traditional Nose Jewellery', ['Nathni', 'Bulak', 'Phuli', 'Moti Phuli', 'Hira Phuli', 'Gemstone Phuli', 'Nathuri / Nathori', 'Nose Ring', 'Nose Pin']],
  ['Bridal Nath', ['Bridal Nath', 'Large Bridal Nath', 'Nath with Chain', 'Pearl Bridal Nath', 'Kundan Bridal Nath', 'Polki Bridal Nath', 'Jadau Nath']],
  ['Design-Based Nath', ['Kundan Nath', 'Polki Nath', 'Jadau Nath', 'Meenakari Nath', 'Pearl Nath', 'Diamond Nath', 'Floral Nath', 'Peacock / Mor Nath', 'Paan Nath', 'Lotus / Kamal Nath']]
].map(([name, children], index) => ({ id: `nath-${index}`, name, active: true, children: children.map((child, childIndex) => ({ id: `nath-${index}-${childIndex}`, name: child, active: true })) }));
const nathProducts = [
  ['Traditional Marathi Nath', 'Marathi Nath', 'Maharashtrian Nath', 'NATH-MAR-001', 0, 'Maharashtra', 'Traditional', 'Gold, Pearl'],
  ['Peshwai Nath with Chain', 'Peshwai Nath', 'Maharashtrian Nath', 'NATH-PES-001', 8, 'Maharashtra', 'Bridal', 'Gold, Pearl'],
  ['Rajputi Heirloom Nath', 'Rajputi Nath', 'Rajasthani Nath', 'NATH-RAJ-001', 5, 'Rajasthan', 'Traditional', 'Gold, Polki'],
  ['Bengali Nathni', 'Bengali Nathni', 'North Indian Nath', 'NATH-BEN-001', 9, 'Bengal', 'Bridal', 'Gold, Pearl'],
  ['Mukkuthi Gold Stud', 'Mukkuthi / Mookkuthi', 'South Indian Nose Jewellery', 'NATH-MUK-001', 3, 'South India', 'Daily Wear', 'Gold'],
  ['Pearl Bridal Nath', 'Pearl Bridal Nath', 'Bridal Nath', 'NATH-BRI-001', 6, 'Maharashtra', 'Bridal', 'Gold, Pearl'],
  ['Kundan Nath', 'Kundan Nath', 'Design-Based Nath', 'NATH-KUN-001', 10, 'Rajasthan', 'Bridal', 'Gold, Kundan'],
  ['Diamond Floral Nath', 'Diamond Nath', 'Design-Based Nath', 'NATH-DIA-001', 11, 'Maharashtra', 'Modern', 'Gold, Diamond']
].map(([name, subcategory, category, sku, imageIndex, region, style, material]) => ({ name, subcategory, category, sku, image: images[imageIndex], region, style, material, description: `${subcategory} design from the RR Gold Nath collection.` }));
const nathItemDefaults = { price: 25000, weight: 4.2, purity: '22K', metal: 'Gold', stoneType: 'Pearl', featured: false, active: true, displayOrder: 0 };
const storedNathProducts = JSON.parse(localStorage.getItem('rr-nath-products') || 'null') || nathProducts.map(product => ({ ...product, active: true }));
storedNathProducts.forEach(product => Object.assign(product, nathItemDefaults, product, { stoneType: product.stoneType || product.material }));
const storedNath = JSON.parse(localStorage.getItem('rr-nath-categories') || 'null') || nathSeed;
const stored = JSON.parse(localStorage.getItem('rr-about') || 'null') || { ...defaultAbout };
const adminCredentials = JSON.parse(localStorage.getItem('rr-admin-credentials') || 'null') || { username: 'RRAdmin', password: 'RRAdmin@1234' };
const app = document.querySelector('#app');
function syncNathProductSource() { nathProducts.splice(0, nathProducts.length, ...storedNathProducts.filter(product => product.active !== false)); }
function formatPrice(price) { return new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', minimumFractionDigits: 0 }).format(Number(price) || 0); }
function header(route) { return `<header class="site-header"><a class="brand" href="#/">RR GOLD NATH<small>JEWELLERY SINCE 1960</small></a><button class="menu" aria-label="Open menu">☰</button><nav class="nav">${[['/', 'Home'], ['/about', 'About'], ['/catalogue', 'Catalogue'], ['/contact', 'Contact'], ['/admin', 'Admin']].map(([path, label]) => `<a class="${route === path ? 'active' : ''}" href="#${path}">${label}</a>`).join('')}</nav></header>`; }
function footer() { return '<footer><strong class="brand">RR GOLD NATH</strong><p>Nine generations of tradition. Six decades of trust in Pune.</p></footer>'; }
function productsView() { return nathProductsView(); }
function nathProductsView(items = storedNathProducts.filter(product => product.active !== false)) { return `<div class="grid nath-grid">${items.map(product => `<a class="product" href="#/product/${encodeURIComponent(product.name)}"><img src="${product.image}" alt="${product.name}" loading="lazy"><div class="product-info"><h3>${product.name}</h3><p>${product.subcategory} · ${product.purity} ${product.metal}</p><strong>${formatPrice(product.price)}</strong><small>${product.sku} · ${product.weight}g</small></div></a>`).join('')}</div>`; }
function nathDetailsView(items) { return `<div class="nath-detail-list">${items.map(product => `<a class="nath-detail-row" href="#/product/${encodeURIComponent(product.name)}"><img src="${product.image}" alt="${product.name}" loading="lazy"><span><strong>${product.name}</strong><small>${product.sku}</small></span><span>${product.subcategory}</span><span>${formatPrice(product.price)} · ${product.weight}g</span><span>${product.region || 'Not specified'}</span><span>${product.purity} ${product.metal} · ${product.stoneType}</span></a>`).join('')}</div>`; }
function nathLoadingView() { return '<div class="nath-loading" role="status"><span></span><span></span><span></span><em>Loading Kundan Nath products</em></div>'; }
function home() { return `<main><section class="hero"><div class="hero-content"><div class="eyebrow">RR Gold Nath · Pune</div><h1>Jewellery with a memory of its own.</h1><p>Traditional craftsmanship, carried through nine generations and shaped for the way you live today.</p><div class="actions"><a class="button primary" href="#/catalogue">Explore Catalogue</a><a class="button" href="#/about">Our Story</a></div></div></section><section class="container"><div class="intro"><div class="intro-image"></div><div class="intro-copy"><div class="eyebrow">A family tradition</div><h2>Made to become part of your story.</h2><p>From Barmeda, Rajasthan to Pune, our work is guided by an enduring respect for gold, detail, and the people who wear it. Every design brings a little of that inheritance forward.</p><a class="button" href="#/about">Read about us</a></div></div><div class="stats"><div class="stat"><strong>9</strong><span>Generations</span></div><div class="stat"><strong>1960</strong><span>Serving Pune since</span></div><div class="stat"><strong>1</strong><span>Family promise</span></div></div></section><section class="container" style="padding-top:20px"><div class="section-head"><div><div class="eyebrow">Selected pieces</div><h2>Quietly unforgettable.</h2></div><a class="button" href="#/catalogue">View all</a></div>${productsView()}</section></main>${footer()}`; }
function about() { return `<main><section class="about-hero"><div class="eyebrow">Our story</div><h1>${stored.title}</h1><p>${stored.subtitle}</p></section><section class="container"><div class="intro-copy"><div class="eyebrow">Nine generations</div><h2>Rooted in Rajasthan. At home in Pune.</h2><p>${stored.history}</p></div><div class="timeline">${timeline.map((item, index) => `<div class="timeline-item"><span>0${index + 1}</span><strong>${item}</strong></div>`).join('')}</div><div class="section-head"><div><div class="eyebrow">What guides us</div><h2>Our values</h2></div></div><div class="values">${values.map(([title, text]) => `<article class="value"><h3>${title}</h3><p>${text}</p></article>`).join('')}</div><div class="highlight"><p>“${stored.statement}”</p></div><div class="section-head"><div><div class="eyebrow">The next chapter</div><h2>Discover our collection.</h2><p>Explore jewellery that brings together generations of tradition and timeless design.</p></div><div class="actions"><a class="button primary" href="#/catalogue">Explore Catalogue</a><a class="button" href="#/contact">Contact Us</a></div></div></section></main>${footer()}`; }
function catalogue() { return `<main><section class="about-hero"><div class="eyebrow">The collection</div><h1>Pieces with presence.</h1><p>Explore designs shaped by heritage, made for now.</p></section><section class="container"><div class="section-head"><div><div class="eyebrow">${products.length} pieces</div><h2>All jewellery</h2></div></div>${productsView()}</section></main>${footer()}`; }
function nath() { const categories = storedNath.filter(category => category.active); const subcategories = categories.flatMap(category => category.children.filter(child => child.active)); const activeProducts = storedNathProducts.filter(product => product.active !== false); return `<main><section class="about-hero"><div class="eyebrow">Nath & Nose Jewellery</div><h1>Nath & Nose Jewellery</h1><p>Traditional Indian nose jewellery crafted for timeless occasions.</p></section><section class="container nath-page"><div class="nath-toolbar"><input class="field" id="nath-search" placeholder="Search Marathi, Peshwai, Kundan, Pearl..."><select class="field" id="nath-region"><option value="">All regions</option><option>Maharashtra</option><option>Rajasthan</option><option>Punjab</option><option>Bengal</option><option>Kashmir</option><option>South India</option></select><select class="field" id="nath-style"><option value="">All styles</option><option>Traditional</option><option>Bridal</option><option>Modern</option><option>Daily Wear</option></select></div><div class="category-rail"><button class="category-chip active" data-category="">All</button>${categories.map(category => `<button class="category-chip" data-category="${category.name}">${category.name.replace(' Nath', '')}</button>`).join('')}${subcategories.map(category => `<button class="category-chip" data-category="${category.name}">${category.name}</button>`).join('')}</div><div class="section-head"><div><div class="eyebrow" id="nath-count">${activeProducts.length} pieces</div><h2>Designed to be remembered.</h2></div></div><div id="nath-results">${nathProductsView(activeProducts)}</div></section></main>${footer()}`; }
function contact() { return `<main><section class="container"><div class="eyebrow">Begin a conversation</div><h1>Let’s find your piece.</h1><p>Tell us what you are looking for and our Pune team will be in touch.</p><form class="form" id="enquiry"><input class="field" name="name" placeholder="Your name" required><input class="field" name="phone" placeholder="Phone number" required><textarea class="field" name="message" rows="5" placeholder="What would you like to enquire about?"></textarea><button class="button primary" type="submit">Send Enquiry</button><p id="form-status"></p></form></section></main>${footer()}`; }
function adminLogin() { return `<main><section class="container login-page"><div class="eyebrow">RR Gold Nath / Admin</div><h1>Welcome back.</h1><p>Sign in to manage website content and Nath categories.</p><form class="admin-panel form" id="admin-login"><label>Username</label><input class="field" name="username" autocomplete="username" required><label>Password</label><input class="field" type="password" name="password" autocomplete="current-password" required><button class="button primary" type="submit">Sign In</button><p id="login-status"></p></form><button class="button" id="show-password-change" type="button">Change Password</button><form class="admin-panel form password-change-form" id="password-change" hidden><label>Username</label><input class="field" name="username" value="${adminCredentials.username}" readonly><label>Current password</label><input class="field" type="password" name="currentPassword" required><label>New password</label><input class="field" type="password" name="newPassword" minlength="8" required><label>Confirm new password</label><input class="field" type="password" name="confirmPassword" minlength="8" required><button class="button primary" type="submit">Save New Password</button><p id="password-status"></p></form></section></main>${footer()}`; }
function admin() { return `<main><section class="container"><div class="section-head"><div><div class="eyebrow">Website content</div><h1>Content, with continuity.</h1></div><button class="button" id="admin-logout" type="button">Log Out</button></div><p>Manage the About page and the Nath category tree here. This local editor stores content in this browser until a server-backed admin is connected.</p><form class="admin-panel form" id="admin-form"><label>Page title</label><input class="field" name="title" value="${stored.title}"><label>Subtitle</label><input class="field" name="subtitle" value="${stored.subtitle}"><label>Short history</label><textarea class="field" name="history" rows="8">${stored.history}</textarea><label>Heritage statement</label><textarea class="field" name="statement" rows="4">${stored.statement}</textarea><button class="button primary" type="submit">Save About Content</button><p id="admin-status"></p></form><div class="admin-panel nath-admin"><div class="eyebrow">Catalogue / Nath</div><h2>Nath categories</h2><div class="category-tree">${storedNath.filter(category => category.active).map(category => `<div class="tree-parent"><strong>${category.name}</strong>${category.children.filter(child => child.active).map(child => `<span>${child.name}</span>`).join('')}</div>`).join('')}</div><form class="form" id="nath-category-form"><label>Parent category</label><select class="field" name="parent">${storedNath.map(category => `<option>${category.name}</option>`).join('')}</select><label>New subcategory</label><input class="field" name="name" placeholder="Example: Brahmani Nath" required><button class="button" type="submit">Add Subcategory</button><p id="nath-admin-status"></p></form></div></section></main>${footer()}`; }
function product(name) { const item = storedNathProducts.find(entry => entry.name === name && entry.active !== false) || storedNathProducts.find(entry => entry.active !== false) || storedNathProducts[0]; return `<main><section class="container intro"><img class="intro-image" style="object-fit:cover;width:100%" src="${item.image}" alt="${item.name}"><div class="intro-copy"><div class="eyebrow">Nath & Nose Jewellery · ${item.subcategory}</div><h1 style="font-size:clamp(2.5rem,5vw,5rem)">${item.name}</h1><p>${item.description}</p><div class="product-specs"><span><strong>Item Code</strong>${item.sku}</span><span><strong>Price</strong>${formatPrice(item.price)}</span><span><strong>Weight</strong>${item.weight}g</span><span><strong>Purity / Metal</strong>${item.purity} · ${item.metal}</span><span><strong>Stone Type</strong>${item.stoneType}</span><span><strong>Region / Style</strong>${item.region || 'Not specified'} · ${item.style || 'Not specified'}</span></div><div class="actions"><a class="button primary" href="#/contact">Enquire Now</a><a class="button" href="https://wa.me/?text=${encodeURIComponent(`I am enquiring about ${item.name} (${item.sku}) - ${formatPrice(item.price)}`)}" target="_blank" rel="noopener">WhatsApp</a></div></div></section></main>${footer()}`; }
function adminCrudPanel() { return `<section class="admin-panel admin-crud"><div class="eyebrow">Catalogue / Manage</div><h2>Categories</h2><p>Add, rename, hide, or delete categories and subcategories.</p><div class="manage-list">${storedNath.map(category => `<div class="manage-row ${category.active ? '' : 'is-hidden'}"><strong>${category.name}</strong><span class="manage-actions"><button class="button compact" data-action="edit-category" data-id="${category.id}">Update</button><button class="button compact" data-action="toggle-category" data-id="${category.id}">${category.active ? 'Hide' : 'Show'}</button><button class="button compact danger" data-action="delete-category" data-id="${category.id}">Delete</button></span>${category.children.map(child => `<span class="manage-child ${child.active ? '' : 'is-hidden'}">${child.name}<span class="manage-actions"><button class="button compact" data-action="edit-child" data-parent="${category.id}" data-id="${child.id}">Update</button><button class="button compact" data-action="toggle-child" data-parent="${category.id}" data-id="${child.id}">${child.active ? 'Hide' : 'Show'}</button><button class="button compact danger" data-action="delete-child" data-parent="${category.id}" data-id="${child.id}">Delete</button></span></span>`).join('')}</div>`).join('')}</div><form class="form manage-form" id="add-category-form"><h3>Add Category</h3><input class="field" name="name" placeholder="Category name" required><button class="button" type="submit">Add Category</button><p id="category-status"></p></form><div class="products-heading"><div><h2>Products</h2><p>Add, update, hide, or delete Nath catalogue items.</p></div><button class="button primary" id="add-product-shortcut" type="button">Add Product</button></div><div class="manage-list">${storedNathProducts.map(product => `<div class="manage-row ${product.active !== false ? '' : 'is-hidden'}"><strong>${product.name}</strong><small>${product.sku} · ${product.subcategory} · ${formatPrice(product.price)}</small><span class="manage-actions"><button class="button compact" data-action="edit-product" data-id="${product.sku}">Update</button><button class="button compact" data-action="toggle-product" data-id="${product.sku}">${product.active !== false ? 'Hide' : 'Show'}</button><button class="button compact danger" data-action="delete-product" data-id="${product.sku}">Delete</button></span></div>`).join('')}</div><form class="form manage-form" id="add-product-form"><h3>Add Product</h3><input class="field" name="name" placeholder="Product name" required><input class="field" name="sku" placeholder="Item code / SKU" required><input class="field" name="subcategory" placeholder="Subcategory" required><input class="field" name="price" type="number" min="0" placeholder="Price in INR" required><input class="field" name="weight" type="number" min="0" step="0.01" placeholder="Weight in grams"><select class="field" name="purity"><option>22K</option><option>18K</option><option>14K</option></select><input class="field" name="metal" placeholder="Metal, e.g. Gold"><input class="field" name="stoneType" placeholder="Stone type, e.g. Pearl"><input class="field" name="material" placeholder="Material summary"><input class="field" name="image" placeholder="Relative image path, e.g. Images/Products/RN001/front.webp"><textarea class="field" name="description" rows="3" placeholder="Description"></textarea><button class="button primary" type="submit">Save Product</button><p id="product-status"></p></form></section>`; }
function persistNathProducts() { localStorage.setItem('rr-nath-products', JSON.stringify(storedNathProducts)); syncNathProductSource(); }
function refreshAdminCrud() { const current = document.querySelector('.admin-crud'); if (current) { current.outerHTML = adminCrudPanel(); bindAdminCrud(); } }
function bindAdminCrud() { let panel = document.querySelector('.admin-crud'); if (!panel) { const anchor = document.querySelector('.nath-admin'); if (!anchor) return; anchor.insertAdjacentHTML('afterend', adminCrudPanel()); panel = document.querySelector('.admin-crud'); } panel.onclick = event => { const button = event.target.closest('[data-action]'); if (!button) return; const action = button.dataset.action; if (action === 'edit-category') { const item = storedNath.find(category => category.id === button.dataset.id); const name = prompt('Update category name', item.name); if (name) item.name = name; } if (action === 'toggle-category') { const item = storedNath.find(category => category.id === button.dataset.id); item.active = !item.active; } if (action === 'delete-category') { const index = storedNath.findIndex(category => category.id === button.dataset.id); if (index > -1 && confirm('Delete this category and its subcategories?')) storedNath.splice(index, 1); } if (action === 'edit-child') { const parent = storedNath.find(category => category.id === button.dataset.parent); const item = parent.children.find(child => child.id === button.dataset.id); const name = prompt('Update subcategory name', item.name); if (name) item.name = name; } if (action === 'toggle-child') { const parent = storedNath.find(category => category.id === button.dataset.parent); const item = parent.children.find(child => child.id === button.dataset.id); item.active = !item.active; } if (action === 'delete-child') { const parent = storedNath.find(category => category.id === button.dataset.parent); const index = parent.children.findIndex(child => child.id === button.dataset.id); if (index > -1 && confirm('Delete this subcategory?')) parent.children.splice(index, 1); } if (action === 'edit-product') { const item = storedNathProducts.find(product => product.sku === button.dataset.id); const name = prompt('Update product name', item.name); if (name) item.name = name; } if (action === 'toggle-product') { const item = storedNathProducts.find(product => product.sku === button.dataset.id); item.active = item.active === false; } if (action === 'delete-product') { const index = storedNathProducts.findIndex(product => product.sku === button.dataset.id); if (index > -1 && confirm('Delete this product?')) storedNathProducts.splice(index, 1); } localStorage.setItem('rr-nath-categories', JSON.stringify(storedNath)); persistNathProducts(); refreshAdminCrud(); }; const categoryForm = document.querySelector('#add-category-form'); if (categoryForm) categoryForm.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(categoryForm)); storedNath.push({ id: `nath-${Date.now()}`, name: data.name, active: true, children: [] }); localStorage.setItem('rr-nath-categories', JSON.stringify(storedNath)); categoryForm.reset(); refreshAdminCrud(); }; const productForm = document.querySelector('#add-product-form'); if (productForm) productForm.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(productForm)); storedNathProducts.push({ name: data.name, sku: data.sku, subcategory: data.subcategory, category: 'Nath', material: data.material || 'Gold', image: data.image || 'Images/1.jpeg', region: '', style: '', description: `${data.subcategory} design from the RR Gold Nath collection.`, active: true }); persistNathProducts(); productForm.reset(); refreshAdminCrud(); }; }
function addProductShortcut() { const shortcut = document.querySelector('#add-product-shortcut'); const form = document.querySelector('#add-product-form'); if (!shortcut || !form) return; shortcut.onclick = () => form.scrollIntoView({ behavior: 'smooth', block: 'center' }); }
function bindItemFormFields() { const form = document.querySelector('#add-product-form'); if (!form) return; const originalSubmit = form.onsubmit; form.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(form)); if (!data.name || !data.sku || !data.subcategory || !data.price) return; storedNathProducts.push({ name: data.name, sku: data.sku, subcategory: data.subcategory, category: 'Nath', price: Number(data.price), weight: Number(data.weight) || 0, purity: data.purity || '22K', metal: data.metal || 'Gold', stoneType: data.stoneType || 'None', material: data.material || `${data.metal || 'Gold'}, ${data.stoneType || 'None'}`, image: data.image || 'Images/1.jpeg', description: data.description || `${data.subcategory} design from the RR Gold Nath collection.`, region: '', style: '', featured: false, active: true, displayOrder: 0 }); persistNathProducts(); form.reset(); refreshAdminCrud(); bindItemFormFields(); }; }
function render() { const path = location.hash.slice(1) || '/'; const route = path.split('/')[1] ? `/${path.split('/')[1]}` : '/'; const isAdmin = route === '/admin'; let view = route === '/about' ? about() : route === '/catalogue' ? nath() : route === '/contact' ? contact() : isAdmin ? (sessionStorage.getItem('rr-admin-auth') === 'true' ? admin() : adminLogin()) : route === '/product' ? product(decodeURIComponent(path.split('/').slice(2).join('/'))) : home(); app.innerHTML = header(route) + view; document.querySelector('.menu').onclick = () => document.querySelector('.nav').classList.toggle('open'); const enquiry = document.querySelector('#enquiry'); if (enquiry) enquiry.onsubmit = event => { event.preventDefault(); document.querySelector('#form-status').textContent = 'Thank you. Your enquiry has been noted for the RR Gold Nath team.'; enquiry.reset(); }; const loginForm = document.querySelector('#admin-login'); if (loginForm) loginForm.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(loginForm)); if (data.username === adminCredentials.username && data.password === adminCredentials.password) { sessionStorage.setItem('rr-admin-auth', 'true'); render(); bindAdminCrud(); addProductShortcut(); } else { document.querySelector('#login-status').textContent = 'Incorrect username or password.'; } }; const logout = document.querySelector('#admin-logout'); if (logout) logout.onclick = () => { sessionStorage.removeItem('rr-admin-auth'); render(); }; const adminForm = document.querySelector('#admin-form'); if (adminForm) adminForm.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(adminForm)); Object.assign(stored, data); localStorage.setItem('rr-about', JSON.stringify(stored)); document.querySelector('#admin-status').textContent = 'Saved locally. The public About page will use this content now.'; }; const nathForm = document.querySelector('#nath-category-form'); if (nathForm) nathForm.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(nathForm)); const parent = storedNath.find(category => category.name === data.parent); parent.children.push({ id: `nath-${Date.now()}`, name: data.name, active: true }); localStorage.setItem('rr-nath-categories', JSON.stringify(storedNath)); document.querySelector('#nath-admin-status').textContent = `${data.name} added under ${data.parent}.`; nathForm.reset(); }; const nathSearch = document.querySelector('#nath-search'); if (nathSearch) { const updateNath = () => { const search = nathSearch.value.toLowerCase(); const region = document.querySelector('#nath-region').value; const style = document.querySelector('#nath-style').value; const category = document.querySelector('.category-chip.active').dataset.category; const filtered = nathProducts.filter(product => (!search || Object.values(product).join(' ').toLowerCase().includes(search)) && (!region || product.region === region) && (!style || product.style === style) && (!category || product.category === category)); document.querySelector('#nath-results').innerHTML = nathProductsView(filtered); document.querySelector('#nath-count').textContent = `${filtered.length} pieces`; }; nathSearch.oninput = updateNath; document.querySelector('#nath-region').onchange = updateNath; document.querySelector('#nath-style').onchange = updateNath; document.querySelectorAll('.category-chip').forEach(chip => chip.onclick = () => { document.querySelectorAll('.category-chip').forEach(item => item.classList.remove('active')); chip.classList.add('active'); updateNath(); }); } }
function bindNathCategoryDetails() { document.querySelectorAll('.category-chip').forEach(chip => { chip.onclick = () => { document.querySelectorAll('.category-chip').forEach(item => item.classList.remove('active')); chip.classList.add('active'); const selected = chip.dataset.category; const results = document.querySelector('#nath-results'); if (!results) return; results.innerHTML = selected === 'Kundan Nath' ? nathLoadingView() : nathProductsView(storedNathProducts.filter(product => product.active !== false)); setTimeout(() => { const filtered = storedNathProducts.filter(product => product.active !== false && (!selected || product.category === selected || product.subcategory === selected)); results.innerHTML = selected && storedNath.flatMap(parent => parent.children).some(child => child.name === selected) ? nathDetailsView(filtered.filter(product => product.subcategory === selected)) : nathProductsView(filtered); document.querySelector('#nath-count').textContent = `${filtered.length} pieces`; }, selected === 'Kundan Nath' ? 450 : 0); }; }); }
function bindPasswordChange() { const button = document.querySelector('#show-password-change'); const form = document.querySelector('#password-change'); if (!button || !form) return; button.onclick = () => { form.hidden = !form.hidden; }; form.onsubmit = event => { event.preventDefault(); const data = Object.fromEntries(new FormData(form)); const status = document.querySelector('#password-status'); if (data.currentPassword !== adminCredentials.password) { status.textContent = 'Current password is incorrect.'; return; } if (data.newPassword !== data.confirmPassword) { status.textContent = 'New passwords do not match.'; return; } adminCredentials.password = data.newPassword; localStorage.setItem('rr-admin-credentials', JSON.stringify(adminCredentials)); form.reset(); status.textContent = 'Password changed successfully.'; }; }
syncNathProductSource(); window.addEventListener('hashchange', () => { render(); bindAdminCrud(); addProductShortcut(); bindItemFormFields(); bindNathCategoryDetails(); bindPasswordChange(); }); render(); bindAdminCrud(); addProductShortcut(); bindItemFormFields(); bindNathCategoryDetails(); bindPasswordChange();
